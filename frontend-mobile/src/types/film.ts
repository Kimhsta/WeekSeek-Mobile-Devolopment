export interface Film {
    releaseYear: ReactNode;
    trailerUrl(trailerUrl: any): unknown;
    duration: ReactNode;
    genre: ReactNode;
    posterUrl: any;
    id: number;
    title: string;
    description: string;
    poster: string;
    views: number;
  }
  